TZ=UTC
PORT=3333
HOST=localhost
LOG_LEVEL=info
APP_KEY=
NODE_ENV=development
# --- PostgreSQL database ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=root
DB_DATABASE=ink_muse_db
# --- Pollinations.ai image generation ---
# Optional but recommended: a FREE token (no payment) makes generation reliable.
# Create one at https://auth.pollinations.ai and uncomment the line below.
# POLLINATIONS_TOKEN=your-free-token-here
POLLINATIONS_MODEL=flux
APP_REFERRER=tattoo-sketch-generator
